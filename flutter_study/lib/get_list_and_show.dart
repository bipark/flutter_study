import 'package:flutter/material.dart';

class GetListAndShow extends StatelessWidget {

  GetListAndShow();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Show List"),
        ),
        body: Container(
          color: Colors.amber,
        )
    );
  }

}